version: '3.8'

services:
  backend-go:
    build:
      context: ./qr-matrix-service
      dockerfile: Dockerfile
    ports:
      - '8080:8080'
    environment:
      - JWT_SECRET=${JWT_SECRET}
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
